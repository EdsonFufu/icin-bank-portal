import { CheckBookRequest } from './check-book-request.model';

describe('CheckBookRequest', () => {
  it('should create an instance', () => {
    expect(new CheckBookRequest()).toBeTruthy();
  });
});
